<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Dialog_Fullscreen">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="dialog_between_select" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <!-- android:windowBackground换窗口默认背景-->
    <!--  android:windowFrame设置Dialog的windowFrame框为无（去掉白色边框）-->
    <!-- ndroid:windowIsFloating是否浮现在activity之上-->
    <!--  android:windowIsTranslucent是否半透明-->
    <!-- android:backgroundDimEnabled背景是否模糊显示-->
    <style name="chatAudioDialog" parent="@android:style/Theme.Dialog"><!--自定义对话框风格-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!--当我们从 A1 启动 A2 时，A1 从屏幕上消失，这个动画叫做 android:activityOpenExitAnimation-->
    <!--当我们从 A1 启动 A2 时，A2 出现在屏幕上，这个动画叫做 android:activityOpenEnterAnimation-->
    <!--当我们从 A2 退出回到 A1 时，A2 从屏幕上消失，这个叫做 android:activityCloseExitAnimation-->
    <!--当我们从 A2 退出回到 A1 时，A1 出现在屏幕上，这个叫做 android:activityCloseEnterAnimation-->


    <style name="popActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/frame_anim_from_popu_in</item>
        <item name="android:activityOpenExitAnimation">@anim/frame_anim_from_popu_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/frame_anim_from_popu_out</item>
        <!--<item name="android:activityCloseExitAnimation">@anim/frame_anim_from_left</item>-->
    </style>

    <style name="jumpToPopActivityAnimation" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/frame_anim_from_popu_in</item>
        <item name="android:activityOpenExitAnimation">@anim/frame_anim_from_popu_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/frame_anim_from_popu_in</item>
        <item name="android:activityCloseExitAnimation">@anim/frame_anim_from_popu_out</item>
    </style>


    <style name="line_horizontal">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/line_width_or_height</item>
        <item name="android:background">@color/default_line_bg</item>
    </style>

    <style name="line_vertical">
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_width">@dimen/line_width_or_height</item>
        <item name="android:background">@color/default_line_bg</item>
    </style>

    <style name="all_parent_item">
        <item name="android:layout_height">0dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/white</item>
        <item name="android:descendantFocusability">blocksDescendants</item>
        <item name="android:layout_marginBottom">@dimen/line_width_or_height</item>
        <item name="android:orientation">horizontal</item>
    </style>

    <style name="all_parent_item_left_img">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:paddingLeft">@dimen/common_dp_1.5</item>
        <item name="android:layout_marginTop">@dimen/common_dp_0.2</item>
        <item name="android:layout_marginBottom">@dimen/common_dp_0.2</item>
    </style>

    <style name="defaule_button_style" parent="Base.Widget.AppCompat.Button.Small">
        <item name="android:background">@null</item>
        <!--文字大小写控制-->
        <item name="android:textAllCaps">false</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <!-- android:windowBackground换窗口默认背景-->
    <!--  android:windowFrame设置Dialog的windowFrame框为无（去掉白色边框）-->
    <!-- ndroid:windowIsFloating是否浮现在activity之上-->
    <!--  android:windowIsTranslucent是否半透明-->
    <!-- android:backgroundDimEnabled背景是否模糊显示-->
    <style name="dialog_be_full_off_window">
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="dialog_anim_picture_select_preview">
        <item name="android:windowEnterAnimation">@anim/anim_from_center</item>
        <item name="android:windowExitAnimation">@anim/anim_to_center</item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
